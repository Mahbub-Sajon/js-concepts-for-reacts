const numbers = [89, 35, 98, 12];
const student = {
    name : "Alexa",
    age : 11,
    movies : ['Amazon', 'Meril', 'Hit']
}
const about = `My Name is ${student.name} age of ${student.age} has number ${numbers[2]} also liked movies ${student.movies[0]}`;
console.log(about);